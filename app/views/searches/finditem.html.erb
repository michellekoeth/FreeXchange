<h2 id="listhead">Items from FreeCycle</h2>

<div class="general">
  <ul class="shop-list">
    <% @listing.each do |dateoflisting,item| %>
      <% if (dateoflisting != "") && (dateoflisting) %>
        <li class="shop-list">
          <div class="shop-list-name"><%= dateoflisting %><%= item %></div>
        </li>
      <% end %>
    <% end %>
  </ul>
</div>
